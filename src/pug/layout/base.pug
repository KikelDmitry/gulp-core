include config.pug
doctype html

html(lang=config.lang)
    head
        include head.pug
        
    body
        //-mixin examples block
        //- +svg('class', 'anchor', 'title', 'desc')
        h1 Let's begin
        include scripts.pug